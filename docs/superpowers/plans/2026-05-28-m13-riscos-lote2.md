# M13 Riscos Lote 2 (R1–R5) — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar dimensões de impacto JSONB + campos quantitativos ao formulário de Riscos; extrair constante de categorias compartilhada; mover Plano de Ação para o módulo de Riscos com seletor de vínculo Risco/Mudança.

**Architecture:** Tasks sequenciais R1→R5. R1+R2 expandem `GestaoRiscos.jsx` (form + tabela). R3 cria `src/lib/constants.js` e remove duplicação em dois componentes. R4 move `PlanoAcao.jsx` para `/src/components/riscos/`, refatora sua API e adiciona tabs em `GestaoRiscos.jsx`. R5 substitui a prop `pleitoId` por seletor dinâmico de Risco ou Mudança dentro do form de PlanoAcao.

**Tech Stack:** React 18.2 + Vite 6.1, JavaScript (JSX), Tailwind CSS 3.x, shadcn/Radix UI, TanStack React Query 5.x, Supabase PostgreSQL, path alias `@` → `./src`

---

## Mapa de Arquivos

| Arquivo | Ação |
|---------|------|
| `docs/database/supabase-migration-m13-riscos.sql` | CRIAR — todas as alterações de schema |
| `src/lib/constants.js` | CRIAR — `IMPACT_CATEGORIES` |
| `src/pages/RiscosMudancas/GestaoRiscos.jsx` | MODIFICAR — R1, R2, R4 (tabs) |
| `src/components/pleitos/MapaRegistroImpacto.jsx` | MODIFICAR — R3 |
| `src/components/pleitos/RegistroForm.jsx` | MODIFICAR — R3 |
| `src/components/pleitos/PlanoAcao.jsx` | DELETAR (move para riscos/) |
| `src/components/riscos/PlanoAcao.jsx` | CRIAR — R4 + R5 |
| `src/components/pleitos/PleitoDetalhes.jsx` | MODIFICAR — remover tab PlanoAcao |

---

## Task 1 — R1: Migration SQL + `impactos` JSONB em GestaoRiscos

**Arquivos:**
- Criar: `docs/database/supabase-migration-m13-riscos.sql`
- Modificar: `src/pages/RiscosMudancas/GestaoRiscos.jsx`

- [ ] **Passo 1.1 — Criar arquivo de migration SQL**

Criar `docs/database/supabase-migration-m13-riscos.sql` com o seguinte conteúdo:

```sql
-- M13 Riscos — novos campos na tabela riscos
ALTER TABLE riscos ADD COLUMN IF NOT EXISTS impactos      JSONB   DEFAULT '[]'::jsonb;
ALTER TABLE riscos ADD COLUMN IF NOT EXISTS escopo_texto  TEXT;
ALTER TABLE riscos ADD COLUMN IF NOT EXISTS prazo_dias    NUMERIC;
ALTER TABLE riscos ADD COLUMN IF NOT EXISTS valor_impacto NUMERIC;

-- M13 Riscos — PlanoAcao: FKs para Risco e Mudança
ALTER TABLE acoes
  ADD COLUMN IF NOT EXISTS projeto_id          UUID REFERENCES projetos(id)              ON DELETE CASCADE,
  ADD COLUMN IF NOT EXISTS registro_risco_id   UUID REFERENCES riscos(id)               ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS registro_mudanca_id UUID REFERENCES mudancas_contratuais(id) ON DELETE SET NULL;
```

- [ ] **Passo 1.2 — Aplicar migration no Supabase**

Executar o SQL acima via Supabase MCP (`execute_sql`) ou pelo painel do Supabase (SQL Editor). Confirmar que nenhum erro ocorre (colunas podem já existir se `IF NOT EXISTS` estiver presente).

- [ ] **Passo 1.3 — Adicionar import de `Checkbox` em `GestaoRiscos.jsx`**

Na linha 12 (bloco de imports do shadcn), adicionar `Checkbox` ao import existente:

```js
import { Checkbox } from "@/components/ui/checkbox";
```

- [ ] **Passo 1.4 — Expandir `EMPTY_FORM` com `impactos`**

Localizar (linha ~62):
```js
const EMPTY_FORM = {
  codigo: "", descricao: "", categoria: "", probabilidade: 3, impacto: 3,
  status: "Ativo", responsavel: "", plano_resposta: "",
};
```

Substituir por:
```js
const EMPTY_FORM = {
  codigo: "", descricao: "", categoria: "", probabilidade: 3, impacto: 3,
  status: "Ativo", responsavel: "", plano_resposta: "",
  impactos: [],
};
```

- [ ] **Passo 1.5 — Adicionar handler `toggleImpacto`**

Logo após a declaração de `EMPTY_FORM`, antes de `export default function GestaoRiscos()`, NÃO adicionar — o handler é uma função interna do componente. Dentro de `GestaoRiscos`, após os hooks `useMutation`, adicionar:

```js
const toggleImpacto = (dim) =>
  setForm(f => ({
    ...f,
    impactos: f.impactos.includes(dim)
      ? f.impactos.filter(d => d !== dim)
      : [...f.impactos, dim],
  }));
```

- [ ] **Passo 1.6 — Expandir `handleEdit` com `impactos`**

Localizar (linha ~143) o objeto dentro de `setForm({...})` de `handleEdit`. Substituir o bloco completo:

```js
const handleEdit = (risco) => {
  setEditing(risco);
  setForm({
    codigo: risco.codigo || "",
    descricao: risco.descricao || "",
    categoria: risco.categoria || "",
    probabilidade: risco.probabilidade ?? 3,
    impacto: risco.impacto ?? 3,
    status: risco.status || "Ativo",
    responsavel: risco.responsavel || "",
    plano_resposta: risco.plano_resposta || "",
    impactos: risco.impactos || [],
  });
  setShowForm(true);
};
```

- [ ] **Passo 1.7 — Adicionar seção de checkboxes no Dialog do form**

No Dialog (linha ~373), localizar o bloco `col-span-2` do Plano de Resposta:
```jsx
<div className="space-y-1 col-span-2">
  <Label>Plano de Resposta</Label>
  <Textarea value={form.plano_resposta} ...
```

Logo APÓS o fechamento desse `<div>` (após `</div>` do Plano de Resposta), adicionar:

```jsx
<div className="space-y-2 col-span-2">
  <Label>Dimensões de Impacto</Label>
  <div className="flex gap-4 flex-wrap">
    {["Escopo", "Prazo", "Valor"].map(dim => (
      <label key={dim} className="flex items-center gap-2 cursor-pointer">
        <Checkbox
          checked={form.impactos.includes(dim)}
          onCheckedChange={() => toggleImpacto(dim)}
        />
        <span className="text-sm">{dim}</span>
      </label>
    ))}
  </div>
</div>
```

- [ ] **Passo 1.8 — Adicionar coluna "Impactos" na tabela**

Na thead (linha ~272), após o `<th>` de "Categoria" e antes do `<th>` de "P", adicionar:

```jsx
<th className="px-4 py-3 text-left text-xs font-semibold text-muted-foreground">Impactos</th>
```

No tbody, após a `<td>` de categoria (linha ~297), adicionar:

```jsx
<td className="px-4 py-3">
  <div className="flex gap-1 flex-wrap">
    {(r.impactos || []).map(dim => (
      <span key={dim} className="text-xs px-1.5 py-0.5 rounded bg-muted text-muted-foreground font-medium">
        {dim}
      </span>
    ))}
  </div>
</td>
```

- [ ] **Passo 1.9 — Build check**

```bash
npm run build
```

Esperado: saída sem erros de compilação. Se houver erro de import de `Checkbox`, verificar se o arquivo existe em `src/components/ui/checkbox.jsx`.

- [ ] **Passo 1.10 — Commit**

```bash
git add docs/database/supabase-migration-m13-riscos.sql src/pages/RiscosMudancas/GestaoRiscos.jsx
git commit -m "feat(riscos): adicionar impactos JSONB multi-select ao formulário de riscos"
```

---

## Task 2 — R2: Campos `escopo_texto`, `prazo_dias`, `valor_impacto`

**Arquivos:**
- Modificar: `src/pages/RiscosMudancas/GestaoRiscos.jsx`

> Migration SQL já foi aplicado na Task 1.

- [ ] **Passo 2.1 — Expandir `EMPTY_FORM` com os 3 novos campos**

Substituir a declaração de `EMPTY_FORM` (já editada na Task 1) por:

```js
const EMPTY_FORM = {
  codigo: "", descricao: "", categoria: "", probabilidade: 3, impacto: 3,
  status: "Ativo", responsavel: "", plano_resposta: "",
  impactos: [],
  escopo_texto: "", prazo_dias: "", valor_impacto: "",
};
```

- [ ] **Passo 2.2 — Expandir `handleEdit` com os 3 novos campos**

No objeto de `setForm` dentro de `handleEdit` (já editado na Task 1), adicionar ao final do objeto:

```js
escopo_texto:  risco.escopo_texto  || "",
prazo_dias:    risco.prazo_dias    ?? "",
valor_impacto: risco.valor_impacto ?? "",
```

O bloco completo fica:
```js
const handleEdit = (risco) => {
  setEditing(risco);
  setForm({
    codigo: risco.codigo || "",
    descricao: risco.descricao || "",
    categoria: risco.categoria || "",
    probabilidade: risco.probabilidade ?? 3,
    impacto: risco.impacto ?? 3,
    status: risco.status || "Ativo",
    responsavel: risco.responsavel || "",
    plano_resposta: risco.plano_resposta || "",
    impactos: risco.impactos || [],
    escopo_texto:  risco.escopo_texto  || "",
    prazo_dias:    risco.prazo_dias    ?? "",
    valor_impacto: risco.valor_impacto ?? "",
  });
  setShowForm(true);
};
```

- [ ] **Passo 2.3 — Converter campos numéricos em `handleSubmit`**

Localizar `handleSubmit` (linha ~158). Substituir o bloco completo:

```js
const handleSubmit = () => {
  const score = (Number(form.probabilidade) || 3) * (Number(form.impacto) || 3);
  const payload = {
    ...form,
    projeto_id: selectedProjectId,
    probabilidade: Number(form.probabilidade) || 3,
    impacto: Number(form.impacto) || 3,
    score,
    prazo_dias:    form.prazo_dias    !== "" ? Number(form.prazo_dias)    : null,
    valor_impacto: form.valor_impacto !== "" ? Number(form.valor_impacto) : null,
  };
  if (editing) updateMut.mutate({ id: editing.id, data: payload });
  else createMut.mutate(payload);
};
```

- [ ] **Passo 2.4 — Adicionar campos condicionais no Dialog**

Logo após o bloco de checkboxes adicionado na Task 1 (seção "Dimensões de Impacto"), adicionar os campos condicionais:

```jsx
{form.impactos.includes("Escopo") && (
  <div className="space-y-1 col-span-2">
    <Label>Descrição do Impacto no Escopo</Label>
    <Textarea
      value={form.escopo_texto}
      onChange={e => setForm(f => ({ ...f, escopo_texto: e.target.value }))}
      rows={2}
      placeholder="Descreva o que entra ou sai do escopo..."
    />
  </div>
)}
{form.impactos.includes("Prazo") && (
  <div className="space-y-1">
    <Label>Impacto em Prazo (dias)</Label>
    <Input
      type="number"
      value={form.prazo_dias}
      onChange={e => setForm(f => ({ ...f, prazo_dias: e.target.value }))}
      placeholder="+15 ou -5"
    />
  </div>
)}
{form.impactos.includes("Valor") && (
  <div className="space-y-1">
    <Label>Impacto Financeiro (R$)</Label>
    <Input
      type="number"
      value={form.valor_impacto}
      onChange={e => setForm(f => ({ ...f, valor_impacto: e.target.value }))}
      placeholder="+150000 ou -50000"
    />
  </div>
)}
```

- [ ] **Passo 2.5 — Build check**

```bash
npm run build
```

Esperado: sem erros.

- [ ] **Passo 2.6 — Commit**

```bash
git add src/pages/RiscosMudancas/GestaoRiscos.jsx
git commit -m "feat(riscos): adicionar campos escopo_texto, prazo_dias, valor_impacto"
```

---

## Task 3 — R3: Constante `IMPACT_CATEGORIES` compartilhada

**Arquivos:**
- Criar: `src/lib/constants.js`
- Modificar: `src/components/pleitos/MapaRegistroImpacto.jsx`
- Modificar: `src/components/pleitos/RegistroForm.jsx`

- [ ] **Passo 3.1 — Criar `src/lib/constants.js`**

```js
export const IMPACT_CATEGORIES = [
  "Engenharia", "Suprimentos", "Liberação de Área",
  "Escopo", "Planejamento", "Gestão & Comunicação",
  "Recursos", "Produtividade", "Segurança", "Qualidade"
];
```

- [ ] **Passo 3.2 — Atualizar `MapaRegistroImpacto.jsx`**

Remover as linhas 11–15 (declaração local de `CATEGORIES`):
```js
// REMOVER:
const CATEGORIES = [
  "Engenharia", "Suprimentos", "Liberação de Área",
  "Escopo", "Planejamento", "Gestão & Comunicação",
  "Recursos", "Produtividade", "Segurança", "Qualidade"
];
```

Adicionar import no topo do arquivo (após os imports existentes):
```js
import { IMPACT_CATEGORIES as CATEGORIES } from "@/lib/constants";
```

Verificar que o resto do componente usa `CATEGORIES` — nenhuma outra mudança necessária.

- [ ] **Passo 3.3 — Atualizar `RegistroForm.jsx`**

Remover as linhas 17–21 (declaração local de `IMPACTO_CATEGORIES`):
```js
// REMOVER:
const IMPACTO_CATEGORIES = [
  "Engenharia", "Suprimentos", "Escopo", "Planejamento",
  "Recursos", "Produtividade", "Liberação de Área",
  "Segurança", "Qualidade", "Gestão & Comunicação"
];
```

Adicionar import no topo do arquivo (após os imports existentes):
```js
import { IMPACT_CATEGORIES as IMPACTO_CATEGORIES } from "@/lib/constants";
```

> **Atenção:** a ordem dos itens no array em `RegistroForm.jsx` é ligeiramente diferente da ordem em `MapaRegistroImpacto.jsx`. Após a mudança, ambos usarão a mesma ordem definida em `constants.js`. Confirmar que a ordem de `constants.js` está correta (a do `MapaRegistroImpacto` é a referência).

- [ ] **Passo 3.4 — Build check**

```bash
npm run build
```

Esperado: sem erros. Se `CATEGORIES` ou `IMPACTO_CATEGORIES` não resolver, verificar caminho do import.

- [ ] **Passo 3.5 — Commit**

```bash
git add src/lib/constants.js src/components/pleitos/MapaRegistroImpacto.jsx src/components/pleitos/RegistroForm.jsx
git commit -m "refactor: extrair IMPACT_CATEGORIES para src/lib/constants.js"
```

---

## Task 4 — R4: Mover PlanoAcao + tabs em GestaoRiscos + remover de PleitoDetalhes

**Arquivos:**
- Criar: `src/components/riscos/PlanoAcao.jsx` (conteúdo refatorado)
- Deletar: `src/components/pleitos/PlanoAcao.jsx`
- Modificar: `src/pages/RiscosMudancas/GestaoRiscos.jsx`
- Modificar: `src/components/pleitos/PleitoDetalhes.jsx`

- [ ] **Passo 4.1 — Criar diretório e novo `PlanoAcao.jsx`**

Criar `src/components/riscos/PlanoAcao.jsx` com o conteúdo abaixo. Este é o componente refatorado que aceita `projectId` em vez de `pleitoId`, filtra por `projeto_id` e exibe uma coluna "Vínculo" na listagem:

```jsx
import { useState } from "react";
import { entities } from "@/api/supabaseEntities";
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Badge } from "@/components/ui/badge";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Plus, Edit, CheckCircle2, Clock, X, Trash2, Save } from "lucide-react";
import { format } from "date-fns";
import { ptBR } from "date-fns/locale";

const statusColors = {
  Pendente: "bg-muted text-foreground",
  "Em Andamento": "bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300",
  Concluída: "bg-status-positive/15 text-status-positive",
  Atrasada: "bg-status-critical/15 text-status-critical",
  Cancelada: "bg-muted text-foreground",
};

const emptyForm = {
  descricao: "",
  formato_tratativa: "Reunião",
  data_inicio_prevista: "",
  data_fim_prevista: "",
  responsavel: "",
  status: "Pendente",
  observacoes: "",
  registro_risco_id: null,
  registro_mudanca_id: null,
};

export default function PlanoAcao({ projectId }) {
  const [showForm, setShowForm] = useState(false);
  const [editingAcao, setEditingAcao] = useState(null);
  const [formData, setFormData] = useState(emptyForm);
  const [vinculoTipo, setVinculoTipo] = useState("risco");
  const queryClient = useQueryClient();

  const { data: acoes = [] } = useQuery({
    queryKey: ["acoes", projectId],
    queryFn: () => entities.Acao.filter({ projeto_id: projectId }),
    enabled: !!projectId,
  });

  const { data: riscos = [] } = useQuery({
    queryKey: ["riscos", projectId],
    queryFn: () => entities.Risco.filter({ projeto_id: projectId }),
    enabled: !!projectId,
  });

  const { data: mudancas = [] } = useQuery({
    queryKey: ["mudancas_contratuais", projectId],
    queryFn: () => entities.MudancaContratual.filter({ projeto_id: projectId }),
    enabled: !!projectId,
  });

  const createAcaoMutation = useMutation({
    mutationFn: (data) => entities.Acao.create({ ...data, projeto_id: projectId }),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["acoes"] });
      setShowForm(false);
      setFormData(emptyForm);
      setVinculoTipo("risco");
    },
  });

  const updateAcaoMutation = useMutation({
    mutationFn: ({ id, data }) => entities.Acao.update(id, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ["acoes"] });
      setEditingAcao(null);
      setShowForm(false);
      setFormData(emptyForm);
      setVinculoTipo("risco");
    },
  });

  const deleteAcaoMutation = useMutation({
    mutationFn: (id) => entities.Acao.delete(id),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ["acoes"] }),
  });

  const handleSubmit = (e) => {
    e.preventDefault();
    const payload = {
      ...formData,
      projeto_id: projectId,
      registro_risco_id:   vinculoTipo === "risco"   ? formData.registro_risco_id   : null,
      registro_mudanca_id: vinculoTipo === "mudanca" ? formData.registro_mudanca_id : null,
    };
    if (editingAcao) {
      updateAcaoMutation.mutate({ id: editingAcao.id, data: payload });
    } else {
      createAcaoMutation.mutate(payload);
    }
  };

  const handleEdit = (acao) => {
    setEditingAcao(acao);
    setVinculoTipo(acao.registro_mudanca_id ? "mudanca" : "risco");
    setFormData({
      descricao: acao.descricao,
      formato_tratativa: acao.formato_tratativa || "Reunião",
      data_inicio_prevista: acao.data_inicio_prevista || "",
      data_fim_prevista: acao.data_fim_prevista || "",
      responsavel: acao.responsavel || "",
      status: acao.status,
      observacoes: acao.observacoes || "",
      registro_risco_id:   acao.registro_risco_id   || null,
      registro_mudanca_id: acao.registro_mudanca_id || null,
    });
    setShowForm(true);
  };

  const handleCancel = () => {
    setShowForm(false);
    setEditingAcao(null);
    setFormData(emptyForm);
    setVinculoTipo("risco");
  };

  const getVinculoLabel = (acao) => {
    if (acao.registro_risco_id) {
      const r = riscos.find(x => x.id === acao.registro_risco_id);
      return r ? `Risco: ${r.codigo || r.descricao?.slice(0, 30)}` : "Risco vinculado";
    }
    if (acao.registro_mudanca_id) {
      const m = mudancas.find(x => x.id === acao.registro_mudanca_id);
      return m ? `Mudança: ${m.titulo?.slice(0, 30)}` : "Mudança vinculada";
    }
    return "—";
  };

  const acoesCompletas = acoes.filter((a) => a.status === "Concluída").length;
  const acoesPendentes = acoes.filter((a) => ["Pendente", "Em Andamento"].includes(a.status)).length;
  const acoesAtrasadas = acoes.filter((a) => a.status === "Atrasada").length;

  return (
    <div className="space-y-6 p-6">
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <Card className="border-blue-200 bg-blue-50 dark:border-blue-800 dark:bg-blue-900/20">
          <CardContent className="p-4 flex items-center justify-between">
            <div>
              <p className="text-sm text-muted-foreground">Pendentes</p>
              <p className="text-2xl font-bold text-blue-900 dark:text-blue-300">{acoesPendentes}</p>
            </div>
            <Clock className="w-8 h-8 text-blue-600 dark:text-blue-400" />
          </CardContent>
        </Card>
        <Card className="border-green-200 bg-green-50 dark:border-green-800 dark:bg-green-900/20">
          <CardContent className="p-4 flex items-center justify-between">
            <div>
              <p className="text-sm text-muted-foreground">Concluídas</p>
              <p className="text-2xl font-bold text-green-900 dark:text-green-300">{acoesCompletas}</p>
            </div>
            <CheckCircle2 className="w-8 h-8 text-green-600 dark:text-green-400" />
          </CardContent>
        </Card>
        <Card className="border-red-200 bg-red-50 dark:border-red-800 dark:bg-red-900/20">
          <CardContent className="p-4 flex items-center justify-between">
            <div>
              <p className="text-sm text-muted-foreground">Atrasadas</p>
              <p className="text-2xl font-bold text-red-900 dark:text-red-300">{acoesAtrasadas}</p>
            </div>
            <X className="w-8 h-8 text-red-600 dark:text-red-400" />
          </CardContent>
        </Card>
      </div>

      <Card className="border-0 shadow-md">
        <CardHeader className="flex flex-row items-center justify-between">
          <CardTitle className="flex items-center gap-2">
            <CheckCircle2 className="w-5 h-5 text-blue-600" />
            Plano de Ação
          </CardTitle>
          <Button
            onClick={() => { setEditingAcao(null); setFormData(emptyForm); setVinculoTipo("risco"); setShowForm(!showForm); }}
            size="sm"
          >
            <Plus className="w-4 h-4 mr-2" />
            Nova Ação
          </Button>
        </CardHeader>
        <CardContent>
          {showForm && (
            <form onSubmit={handleSubmit} className="space-y-4 mb-6 p-4 border border-border rounded-lg bg-muted">
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">

                {/* Vínculo — Risco ou Mudança */}
                <div className="space-y-2 md:col-span-2">
                  <Label>Vincular a</Label>
                  <div className="flex gap-4 mb-2">
                    {["risco", "mudanca"].map(tipo => (
                      <label key={tipo} className="flex items-center gap-2 cursor-pointer">
                        <input
                          type="radio"
                          name="vinculo-tipo"
                          value={tipo}
                          checked={vinculoTipo === tipo}
                          onChange={() => {
                            setVinculoTipo(tipo);
                            setFormData(f => ({ ...f, registro_risco_id: null, registro_mudanca_id: null }));
                          }}
                        />
                        <span className="text-sm">{tipo === "risco" ? "Risco" : "Mudança"}</span>
                      </label>
                    ))}
                  </div>
                  <Select
                    value={
                      vinculoTipo === "risco"
                        ? formData.registro_risco_id || "__none__"
                        : formData.registro_mudanca_id || "__none__"
                    }
                    onValueChange={(v) => {
                      const id = v === "__none__" ? null : v;
                      setFormData(f => vinculoTipo === "risco"
                        ? { ...f, registro_risco_id: id, registro_mudanca_id: null }
                        : { ...f, registro_risco_id: null, registro_mudanca_id: id }
                      );
                    }}
                  >
                    <SelectTrigger><SelectValue placeholder="Selecione..." /></SelectTrigger>
                    <SelectContent>
                      <SelectItem value="__none__">Nenhum</SelectItem>
                      {(vinculoTipo === "risco" ? riscos : mudancas).map(item => (
                        <SelectItem key={item.id} value={item.id}>
                          {item.codigo || item.titulo || item.descricao?.slice(0, 50) || item.id}
                        </SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                </div>

                <div className="space-y-2 md:col-span-2">
                  <Label>Descrição da Ação *</Label>
                  <Textarea
                    value={formData.descricao}
                    onChange={(e) => setFormData({ ...formData, descricao: e.target.value })}
                    placeholder="Descreva a ação..." required rows={3}
                  />
                </div>

                <div className="space-y-2">
                  <Label>Formato da Tratativa</Label>
                  <Select value={formData.formato_tratativa} onValueChange={(v) => setFormData({ ...formData, formato_tratativa: v })}>
                    <SelectTrigger><SelectValue /></SelectTrigger>
                    <SelectContent>
                      <SelectItem value="Reunião">Reunião</SelectItem>
                      <SelectItem value="Documento">Documento</SelectItem>
                      <SelectItem value="Inspeção">Inspeção</SelectItem>
                      <SelectItem value="Análise Técnica">Análise Técnica</SelectItem>
                      <SelectItem value="Negociação">Negociação</SelectItem>
                      <SelectItem value="Outros">Outros</SelectItem>
                    </SelectContent>
                  </Select>
                </div>

                <div className="space-y-2">
                  <Label>Status</Label>
                  <Select value={formData.status} onValueChange={(v) => setFormData({ ...formData, status: v })}>
                    <SelectTrigger><SelectValue /></SelectTrigger>
                    <SelectContent>
                      <SelectItem value="Pendente">Pendente</SelectItem>
                      <SelectItem value="Em Andamento">Em Andamento</SelectItem>
                      <SelectItem value="Concluída">Concluída</SelectItem>
                      <SelectItem value="Atrasada">Atrasada</SelectItem>
                      <SelectItem value="Cancelada">Cancelada</SelectItem>
                    </SelectContent>
                  </Select>
                </div>

                <div className="space-y-2">
                  <Label>Data Início Prevista</Label>
                  <Input type="date" value={formData.data_inicio_prevista}
                    onChange={(e) => setFormData({ ...formData, data_inicio_prevista: e.target.value })} />
                </div>

                <div className="space-y-2">
                  <Label>Data Fim Prevista</Label>
                  <Input type="date" value={formData.data_fim_prevista}
                    onChange={(e) => setFormData({ ...formData, data_fim_prevista: e.target.value })} />
                </div>

                <div className="space-y-2 md:col-span-2">
                  <Label>Responsável</Label>
                  <Input value={formData.responsavel}
                    onChange={(e) => setFormData({ ...formData, responsavel: e.target.value })}
                    placeholder="Nome do responsável" />
                </div>

                <div className="space-y-2 md:col-span-2">
                  <Label>Observações</Label>
                  <Textarea value={formData.observacoes}
                    onChange={(e) => setFormData({ ...formData, observacoes: e.target.value })}
                    placeholder="Observações adicionais..." rows={2} />
                </div>
              </div>

              <div className="flex justify-end gap-2">
                <Button type="button" variant="outline" onClick={handleCancel}>
                  Cancelar
                </Button>
                <Button type="submit" variant="save"
                  disabled={createAcaoMutation.isPending || updateAcaoMutation.isPending}>
                  <Save className="w-4 h-4 mr-2" />
                  {createAcaoMutation.isPending || updateAcaoMutation.isPending ? "Salvando..." : "Salvar Ação"}
                </Button>
              </div>
            </form>
          )}

          {acoes.length === 0 ? (
            <div className="text-center py-12 text-muted-foreground">
              Nenhuma ação registrada. Clique em &quot;Nova Ação&quot; para começar.
            </div>
          ) : (
            <div className="overflow-x-auto">
              <Table>
                <TableHeader>
                  <TableRow className="bg-muted">
                    <TableHead>Descrição</TableHead>
                    <TableHead>Vínculo</TableHead>
                    <TableHead>Responsável</TableHead>
                    <TableHead>Previsão</TableHead>
                    <TableHead>Status</TableHead>
                    <TableHead>Ações</TableHead>
                  </TableRow>
                </TableHeader>
                <TableBody>
                  {acoes.map((acao) => (
                    <TableRow key={acao.id} className="hover:bg-muted">
                      <TableCell className="max-w-md">
                        <p className="font-medium text-foreground line-clamp-2">{acao.descricao}</p>
                        <p className="text-xs text-muted-foreground mt-1">{acao.formato_tratativa}</p>
                      </TableCell>
                      <TableCell className="text-xs text-muted-foreground max-w-[160px]">
                        {getVinculoLabel(acao)}
                      </TableCell>
                      <TableCell className="text-sm">{acao.responsavel || "-"}</TableCell>
                      <TableCell className="text-sm text-muted-foreground">
                        {acao.data_fim_prevista
                          ? format(new Date(acao.data_fim_prevista), "dd/MM/yyyy", { locale: ptBR })
                          : "-"}
                      </TableCell>
                      <TableCell>
                        <Badge variant="outline" className={statusColors[acao.status]}>{acao.status}</Badge>
                      </TableCell>
                      <TableCell>
                        <div className="flex items-center gap-1">
                          <Button size="sm" variant="outline" className="text-muted-foreground border-border"
                            onClick={() => handleEdit(acao)}>
                            <Edit className="w-4 h-4" />
                          </Button>
                          <Button size="sm" variant="outline" className="text-status-critical border-status-critical/30 hover:bg-status-critical/10"
                            onClick={() => deleteAcaoMutation.mutate(acao.id)}>
                            <Trash2 className="w-4 h-4" />
                          </Button>
                        </div>
                      </TableCell>
                    </TableRow>
                  ))}
                </TableBody>
              </Table>
            </div>
          )}
        </CardContent>
      </Card>
    </div>
  );
}
```

- [ ] **Passo 4.2 — Remover o arquivo antigo**

```bash
rm src/components/pleitos/PlanoAcao.jsx
```

- [ ] **Passo 4.3 — Adicionar tabs em `GestaoRiscos.jsx`**

No início do componente `GestaoRiscos`, logo após as declarações de estado existentes, adicionar:

```js
const [tab, setTab] = useState("riscos");
```

Também adicionar o import do novo `PlanoAcao`:
```js
import PlanoAcao from "@/components/riscos/PlanoAcao";
```

Localizar a div com `className="flex-1 overflow-auto p-6 space-y-6"` (linha ~195) que contém os KPIs, matriz, filtros e tabela. Substituir esse bloco (tudo dentro do `<div className="flex flex-col h-full">` externo, após o `<PageHeader>`) por:

```jsx
{/* Tabs */}
<div className="flex gap-0 border-b border-border px-6 flex-shrink-0">
  {[
    { key: "riscos", label: "Riscos" },
    { key: "plano-acao", label: "Plano de Ação" },
  ].map(t => (
    <button
      key={t.key}
      onClick={() => setTab(t.key)}
      className={`px-5 py-3 text-sm font-medium border-b-2 -mb-px transition-colors ${
        tab === t.key
          ? "border-emerald-600 text-emerald-700 dark:text-emerald-400"
          : "border-transparent text-muted-foreground hover:text-foreground"
      }`}
    >
      {t.label}
    </button>
  ))}
</div>

{tab === "riscos" && (
  <div className="flex-1 overflow-auto p-6 space-y-6">
    {/* KPIs, Matriz, FilterBar, Tabela e Dialog ficam aqui */}
  </div>
)}
{tab === "plano-acao" && (
  <div className="flex-1 overflow-auto">
    <PlanoAcao projectId={selectedProjectId} />
  </div>
)}
```

**Instrução precisa para mover o conteúdo:** Em `GestaoRiscos.jsx`, o trecho que hoje está dentro de `<div className="flex-1 overflow-auto p-6 space-y-6">` contém (nessa ordem):
1. Grid de KPIs (`<div className="grid grid-cols-2 md:grid-cols-4 ...">`)
2. `<Card>` da Matriz 5×5
3. `<FilterBar .../>`
4. `<div className="bg-card rounded-xl border ...">` (tabela)
5. `<Dialog open={showForm} ...>` (modal de form)

Todo esse conteúdo deve ser movido para dentro de `{tab === "riscos" && <div className="flex-1 overflow-auto p-6 space-y-6"> ... </div>}`.

O `<ImportExportDialog>` fica **fora** do bloco de tabs (logo antes do fechamento do `<div className="flex flex-col h-full">` mais externo), pois é acionado pelo botão no `PageHeader` independentemente da tab ativa.

- [ ] **Passo 4.4 — Remover PlanoAcao de `PleitoDetalhes.jsx`**

Em `src/components/pleitos/PleitoDetalhes.jsx`:

1. Remover linha 10: `import PlanoAcao from "./PlanoAcao";`

2. Na `TabsList` (linha ~55), mudar de `grid-cols-3` para `grid-cols-2`:
```jsx
<TabsList className="grid w-full grid-cols-2 lg:w-auto lg:grid-cols-2">
```

3. Remover completamente o `TabsTrigger` de "acoes" (linhas 60–63):
```jsx
// REMOVER:
<TabsTrigger value="acoes" className="flex items-center gap-2">
  <CheckSquare className="w-4 h-4" />
  <span className="hidden sm:inline">Plano de Ação</span>
</TabsTrigger>
```

4. Remover completamente o `TabsContent` de "acoes" (linhas 121–123):
```jsx
// REMOVER:
<TabsContent value="acoes">
  <PlanoAcao pleitoId={pleito.id} />
</TabsContent>
```

5. Remover `CheckSquare` do import de lucide-react se não usado em outro lugar. Verificar o arquivo.

- [ ] **Passo 4.5 — Build check**

```bash
npm run build
```

Esperado: sem erros. Se houver "cannot find module PlanoAcao", verificar se o arquivo antigo foi removido e o novo criado corretamente.

- [ ] **Passo 4.6 — Commit**

```bash
git add src/components/riscos/PlanoAcao.jsx src/pages/RiscosMudancas/GestaoRiscos.jsx src/components/pleitos/PleitoDetalhes.jsx
git rm src/components/pleitos/PlanoAcao.jsx
git commit -m "feat(riscos): mover PlanoAcao para módulo de Riscos com tabs e FKs registro_risco_id/registro_mudanca_id"
```

---

## Task 5 — R5: Verificação e polish do seletor Risco/Mudança

> O seletor foi implementado na Task 4 dentro do novo `PlanoAcao.jsx`. Esta task verifica o comportamento e faz ajustes finais.

**Arquivos:**
- Verificar: `src/components/riscos/PlanoAcao.jsx`

- [ ] **Passo 5.1 — Verificar cenários de estado do seletor**

Confirmar no código de `PlanoAcao.jsx` que:

a) Ao clicar "Nova Ação", `vinculoTipo` é "risco" e ambas as FKs são `null` em `emptyForm`. ✓ (já implementado)

b) Ao editar ação com `registro_mudanca_id` preenchido, `vinculoTipo` é inicializado como "mudanca". ✓ (handleEdit usa `acao.registro_mudanca_id ? "mudanca" : "risco"`)

c) Ao trocar o radio (risco ↔ mudança), ambas as FKs são zeradas. ✓ (onChange do radio)

d) `handleSubmit` envia apenas a FK correspondente ao `vinculoTipo` atual, zerando a outra. ✓ (payload condicional)

- [ ] **Passo 5.2 — Verificar coluna "Vínculo" na tabela**

Confirmar que `getVinculoLabel` retorna corretamente:
- Para ação com `registro_risco_id`: busca em `riscos` e retorna `"Risco: <codigo>"` ou `"Risco: <descricao truncada>"`
- Para ação com `registro_mudanca_id`: busca em `mudancas` e retorna `"Mudança: <titulo truncado>"`
- Para ação sem vínculo: retorna `"—"`

Se `riscos` ou `mudancas` ainda não carregaram, a função retorna o fallback `"Risco vinculado"` / `"Mudança vinculada"`. Isso é aceitável.

- [ ] **Passo 5.3 — Build check final**

```bash
npm run build
```

Esperado: saída limpa, sem warnings de variáveis não usadas ou imports quebrados.

- [ ] **Passo 5.4 — Verificação manual no browser**

```bash
npm run dev
```

1. Navegar para `/riscos-mudancas/gestao-riscos`
2. Confirmar que as tabs "Riscos" e "Plano de Ação" aparecem
3. Na tab Riscos: criar um novo risco, marcar dimensões "Escopo" e "Prazo" → campos condicionais aparecem ✓
4. Na tab Plano de Ação: criar uma nova ação → seletor de Risco/Mudança aparece, trocar radio, selecionar item ✓
5. Salvar ação e confirmar que a coluna "Vínculo" exibe o risco/mudança correto ✓
6. Editar a ação → `vinculoTipo` inicializa corretamente ✓
7. Navegar para `/admin-contratual/pleitos` → abrir um pleito → confirmar que a tab "Plano de Ação" **não** aparece mais ✓

- [ ] **Passo 5.5 — Commit final**

```bash
git add src/components/riscos/PlanoAcao.jsx
git commit -m "feat(riscos): seletor Risco/Mudança no Plano de Ação substituindo pleito_id"
```

---

## Checklist de Cobertura da Spec

| Req | Implementado em |
|-----|----------------|
| R1 — `impactos JSONB` multi-select (Escopo/Prazo/Valor) | Task 1 |
| R1 — coluna "Impactos" na tabela | Task 1 |
| R2 — `escopo_texto`, `prazo_dias`, `valor_impacto` no form | Task 2 |
| R2 — campos condicionais (só aparecem se dimensão marcada) | Task 2 |
| R3 — `IMPACT_CATEGORIES` em `constants.js` | Task 3 |
| R3 — MapaRegistroImpacto importa de constants | Task 3 |
| R3 — RegistroForm importa de constants | Task 3 |
| R4 — PlanoAcao movido para `/src/components/riscos/` | Task 4 |
| R4 — tabs em GestaoRiscos | Task 4 |
| R4 — PlanoAcao removido de PleitoDetalhes | Task 4 |
| R4 — migration SQL (`registro_risco_id`, `registro_mudanca_id`, `projeto_id`) | Task 1 |
| R5 — seletor Risco/Mudança no form | Task 4 (PlanoAcao.jsx) |
| R5 — `vinculoTipo` inicializado corretamente ao editar | Task 4 (handleEdit) |
| R5 — coluna "Vínculo" na listagem | Task 4 (getVinculoLabel) |
